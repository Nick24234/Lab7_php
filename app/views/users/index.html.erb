<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
  <h2>Всі користувачі</h2>
  <%= link_to 'Створити новий акаунт', new_user_path, class: 'btn btn-primary' %>
</div>

<% if @users.any? %>
  <div class="posts-list">
    <% @users.each do |user| %>
      <div class="post-card">
        <h3>👤 <%= user.nickname %></h3>
        <div class="post-meta">
          <strong>💰 Баланс:</strong> <%= number_to_currency(user.balance, unit: "₴", separator: ",", delimiter: " ") %> | 
          <strong>Створено:</strong> <%= user.created_at.strftime("%d.%m.%Y о %H:%M") %>
        </div>
        <div class="post-actions">
          <%= link_to 'Переглянути', user, class: 'btn btn-primary' %>
          <% if user_signed_in? && current_user.id == user.id %>
            <%= link_to 'Редагувати', edit_user_path(user), class: 'btn btn-secondary' %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="empty-state">
    <h2>Поки що немає користувачів</h2>
    <p>Створіть перший акаунт, щоб почати роботу з системою!</p>
    <%= link_to 'Створити акаунт', new_user_path, class: 'btn btn-primary', style: 'margin-top: 20px;' %>
  </div>
<% end %>
